# Veritas AI has no external Python runtime dependencies.
#
# The local server, smoke tests, and Splunk HEC/REST helpers use only the
# Python standard library. Keep this file so CI and reviewers can run:
#
#   python -m pip install -r requirements.txt
#
# Add pinned packages here only when a future feature requires them.
